\documentclass[letterpaper,11pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}
\usepackage{latexsym}
\usepackage[empty]{fullpage}
\usepackage{titlesec}
\usepackage[usenames,dvipsnames]{color}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}
\usepackage{fancyhdr}
\usepackage{ragged2e}
\usepackage{microtype}
\usepackage{parskip}
\usepackage{tabularx}
\usepackage[sfdefault]{noto-sans}
\input{glyphtounicode}

\pagestyle{fancy}
\fancyhf{}
\fancyfoot{}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

% Adjust margins similarly to the resume template
\addtolength{\oddsidemargin}{-0.5in}
\addtolength{\evensidemargin}{-0.5in}
\addtolength{\textwidth}{1in}
\addtolength{\topmargin}{-.5in}
\addtolength{\textheight}{1.0in}

\setlength{\parskip}{0.85em}
\setlength{\parindent}{0pt}
\setlength{\tabcolsep}{0in}

\justifying
\raggedbottom
\pdfgentounicode=1

\titleformat{\section}{\vspace{-4pt}\raggedright\large}{}{0em}{}[\color{black}\titlerule \vspace{-5pt}]

\begin{document}

\begin{center}
  \textbf{\Huge\scshape {{profile.name}} } \\\[2pt]
  {{#if profile.title}}
    \small{ {{profile.title}} } \\\[2pt]
  {{/if}}
  {{#if profile.phoneNumbers}}
    \small{ {{#each profile.phoneNumbers}}\underline{ {{this}} }{{#unless @last}}\, {{/unless}}{{/each}} }\\
  {{/if}}
  {{#if profile.emails}}
    \small{ {{#each profile.emails}}\underline{ {{this}} }{{#unless @last}}\, {{/unless}}{{/each}} }\\
  {{/if}}
  {{#if profile.links}}
    \small{ {{#each profile.links}}\href{ {{this.url}} }{\underline{ {{this.type}} }}{{#unless @last}}\, {{/unless}}{{/each}} }}
  {{/if}}
\end{center}

\vspace{12pt}
\rule{\textwidth}{0.4pt}
\vspace{18pt}

\begin{tabularx}{\textwidth}{Xr}
  {{#if coverLetter.senderLocation}}
    {{coverLetter.senderLocation}}
  {{/if}}
  &
  {{#if coverLetter.sentAt}}
    {{coverLetter.sentAt}}
  {{/if}} \\
\end{tabularx}

\vspace{12pt}

{{#if coverLetter.company}}
  \textbf{ {{coverLetter.company}} }\\
{{/if}}
{{#if coverLetter.companyRecipient}}
  {{coverLetter.companyRecipient}}\\
{{/if}}
{{#if coverLetter.companyLocation}}
  {{coverLetter.companyLocation}}\\
{{/if}}

{{#if coverLetter.position}}
  \vspace{16pt}
  \textbf{Re: {{coverLetter.position}}}\\
{{/if}}

\vspace{12pt}
{{#if coverLetter.salutation}}
  {{coverLetter.salutation}}
{{else}}
  Dear Hiring Manager,
{{/if}}

\vspace{8pt}
{{{coverLetter.body}}}

\vspace{18pt}
{{#if coverLetter.closing}}
  {{coverLetter.closing}}\\
{{else}}
  Sincerely,\\
{{/if}}

{{profile.name}}\\
{{#if profile.title}}
  {{profile.title}}\\
{{/if}}

\end{document}
